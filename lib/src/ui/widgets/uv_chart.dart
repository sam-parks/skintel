import 'package:flutter/material.dart';

class UVChart extends StatelessWidget {
  const UVChart({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
